import { insert, createComponent, effect, className, template } from 'solid-js/web';
import { createMemo, For, createSignal } from 'solid-js';
import { unified } from 'unified';
import remarkParse from 'remark-parse';
import remarkGfm from 'remark-gfm';
import remarkRehype from 'remark-rehype';
import { toJsxRuntime } from 'hast-util-to-jsx-runtime';
import { Fragment, jsxs, jsx } from 'solid-js/h/jsx-runtime';

// src/stream-markdown.tsx

// src/utils/parse-incomplete-markdown.ts
function findEndOfCellOrLineContaining(line, position) {
  for (let i = position; i < line.length; i++) {
    if (line[i] === "|") return i;
  }
  return line.length;
}
var IncompleteMarkdownParser = class {
  plugins = [];
  state = {
    currentLine: 0,
    context: "normal",
    blockingContexts: /* @__PURE__ */ new Set(),
    lineContexts: []
  };
  setState = (state) => {
    this.state = { ...this.state, ...state };
  };
  constructor(plugins = []) {
    this.plugins = plugins;
  }
  parse(text) {
    if (!text || typeof text !== "string") {
      return text;
    }
    this.state = {
      currentLine: 0,
      context: "normal",
      blockingContexts: /* @__PURE__ */ new Set(),
      lineContexts: [],
      fenceInfo: void 0
    };
    let result = text;
    for (const plugin of this.plugins) {
      if (plugin.preprocess) {
        try {
          const preprocessResult = plugin.preprocess({
            text: result,
            state: this.state,
            setState: this.setState
          });
          if (typeof preprocessResult === "string") {
            result = preprocessResult;
          } else {
            result = preprocessResult.text;
            this.setState(preprocessResult.state);
          }
        } catch (error) {
          console.error(
            `Plugin ${plugin.name} preprocess hook failed:`,
            error
          );
        }
      }
    }
    const lines = result.split("\n");
    const processedLines = [...lines];
    for (let i = 0; i < processedLines.length; i++) {
      this.state.currentLine = i;
      let line = processedLines[i];
      for (const plugin of this.plugins) {
        const currentLineContext = this.state.lineContexts?.[i];
        const shouldSkip = currentLineContext && (plugin.skipInBlockTypes || []).some(
          (blockType) => currentLineContext[blockType]
        );
        if (shouldSkip) continue;
        try {
          const match = plugin.pattern ? line.match(plugin.pattern) : line.match(/.*/);
          if (match && plugin.handler) {
            line = plugin.handler({
              line,
              text: line,
              match,
              state: this.state,
              setState: this.setState
            });
          }
        } catch (error) {
          console.error(`Plugin ${plugin.name} failed on line ${i}:`, error);
        }
      }
      processedLines[i] = line;
    }
    result = processedLines.join("\n");
    for (const plugin of this.plugins) {
      if (plugin.postprocess) {
        try {
          result = plugin.postprocess({
            text: result,
            state: this.state,
            setState: this.setState
          });
        } catch (error) {
          console.error(
            `Plugin ${plugin.name} postprocess hook failed:`,
            error
          );
        }
      }
    }
    return result;
  }
  static createDefaultPlugins() {
    return [
      // Context manager: tracks code/math blocks across the entire text
      {
        name: "contextManager",
        preprocess: ({ text }) => {
          const lines = text.split("\n");
          let inCodeBlock = false;
          let inMathBlock = false;
          const lineContexts = [];
          for (let i = 0; i < lines.length; i++) {
            const line = lines[i];
            if (line.trim().startsWith("```") || line.trim().startsWith("~~~")) {
              inCodeBlock = !inCodeBlock;
            }
            if (line.trim().startsWith("$$") && !line.trim().includes("$$", 2)) {
              inMathBlock = !inMathBlock;
            }
            lineContexts[i] = {
              code: inCodeBlock,
              math: inMathBlock
            };
          }
          const finalContexts = /* @__PURE__ */ new Set();
          if (inCodeBlock) finalContexts.add("code");
          if (inMathBlock) finalContexts.add("math");
          return {
            text,
            state: {
              blockingContexts: finalContexts,
              lineContexts
            }
          };
        },
        postprocess: ({ text, state }) => {
          if (state.blockingContexts.has("code")) {
            return text + "\n```";
          }
          if (state.blockingContexts.has("math")) {
            return text + "\n$$";
          }
          return text;
        }
      },
      // Bold+italic (***) completion
      {
        name: "boldItalic",
        pattern: /\*\*\*/,
        skipInBlockTypes: ["code", "math"],
        handler: ({ line }) => {
          if (line.trim() === "***") return line;
          const tripleAsterisks = (line.match(/\*\*\*/g) || []).length;
          if (tripleAsterisks % 2 === 1) {
            const lastIdx = line.lastIndexOf("***");
            const end = findEndOfCellOrLineContaining(line, lastIdx);
            if (line.endsWith("***")) {
              return line.substring(0, lastIdx);
            }
            return line.substring(0, end) + "***" + line.substring(end);
          }
          return line;
        }
      },
      // Bold (**) completion
      {
        name: "bold",
        pattern: /\*\*/,
        skipInBlockTypes: ["code", "math"],
        handler: ({ line }) => {
          if (line.trim() === "***") return line;
          const count = (line.match(/\*\*/g) || []).length;
          if (count % 2 === 1) {
            const lastIdx = line.lastIndexOf("**");
            const end = findEndOfCellOrLineContaining(line, lastIdx);
            if (line.endsWith("**")) {
              return line.substring(0, lastIdx);
            }
            return line.substring(0, end) + "**" + line.substring(end);
          }
          return line;
        }
      },
      // Double underscore (__) completion
      {
        name: "doubleUnderscoreItalic",
        pattern: /__/,
        skipInBlockTypes: ["code", "math"],
        handler: ({ line }) => {
          if (line.trim() === "___") return line;
          const count = (line.match(/__/g) || []).length;
          if (count % 2 === 1) {
            const lastIdx = line.lastIndexOf("__");
            const end = findEndOfCellOrLineContaining(line, lastIdx);
            if (line.endsWith("__")) {
              return line.substring(0, lastIdx);
            }
            return line.substring(0, end) + "__" + line.substring(end);
          }
          return line;
        }
      },
      // Strikethrough (~~) completion
      {
        name: "strikethrough",
        pattern: /~~/,
        skipInBlockTypes: ["code", "math"],
        handler: ({ line }) => {
          const count = (line.match(/~~/g) || []).length;
          if (count % 2 === 1) {
            const lastIdx = line.lastIndexOf("~~");
            const end = findEndOfCellOrLineContaining(line, lastIdx);
            const contentAfter = line.substring(lastIdx + 2, end);
            if (contentAfter.trim().length > 0) {
              if (line.endsWith("~~")) {
                return line.substring(0, lastIdx);
              }
              return line.substring(0, end) + "~~" + line.substring(end);
            }
          }
          return line;
        }
      },
      // Single asterisk italic (*) completion
      {
        name: "singleAsteriskItalic",
        pattern: /[\s\S]*/,
        skipInBlockTypes: ["code", "math"],
        handler: ({ line }) => {
          if (line.trim() === "***") return line;
          let singleAsterisks = 0;
          for (let i = 0; i < line.length; i++) {
            if (line[i] === "*") {
              const prevChar = i > 0 ? line[i - 1] : "";
              const nextChar = i < line.length - 1 ? line[i + 1] : "";
              let lineStartIndex = 0;
              for (let j = i - 1; j >= 0; j--) {
                if (line[j] === "\n") {
                  lineStartIndex = j + 1;
                  break;
                }
              }
              const beforeAsterisk = line.substring(lineStartIndex, i);
              if (beforeAsterisk.trim() === "" && (nextChar === " " || nextChar === "	")) {
                continue;
              }
              if (prevChar !== "*" && nextChar !== "*") {
                singleAsterisks++;
              }
            }
          }
          if (singleAsterisks % 2 === 1) {
            let firstIdx = -1;
            for (let i = 0; i < line.length; i++) {
              if (line[i] === "*" && line[i - 1] !== "*" && line[i + 1] !== "*") {
                const prevChar = i > 0 ? line[i - 1] : "";
                const nextChar = i < line.length - 1 ? line[i + 1] : "";
                if (/\w/.test(prevChar) && /\w/.test(nextChar)) continue;
                if (/\w/.test(prevChar) && !/\s/.test(prevChar)) continue;
                firstIdx = i;
                break;
              }
            }
            if (firstIdx !== -1) {
              const end = findEndOfCellOrLineContaining(line, firstIdx);
              return line.substring(0, end) + "*" + line.substring(end);
            }
          }
          return line;
        }
      },
      // Inline code (`) completion
      {
        name: "inlineCode",
        skipInBlockTypes: ["code", "math"],
        pattern: /`/,
        handler: ({ line }) => {
          let singleBacktickCount = 0;
          for (let i = 0; i < line.length; i++) {
            if (line[i] === "`") {
              const isTripleStart = line.substring(i, i + 3) === "```";
              const isTripleMiddle = i > 0 && line.substring(i - 1, i + 2) === "```";
              const isTripleEnd = i > 1 && line.substring(i - 2, i + 1) === "```";
              if (!isTripleStart && !isTripleMiddle && !isTripleEnd) {
                singleBacktickCount++;
              }
            }
          }
          const tripleBackticks = (line.match(/```/g) || []).length;
          const hasCompleteBlock = tripleBackticks > 0 && tripleBackticks % 2 === 0 && line.includes("\n");
          if (singleBacktickCount % 2 === 1 && !hasCompleteBlock) {
            const lastIdx = line.lastIndexOf("`");
            const end = findEndOfCellOrLineContaining(line, lastIdx);
            const contentAfter = line.substring(lastIdx + 1, end);
            if (contentAfter.trim().length > 0 && !contentAfter.includes("|")) {
              return line.substring(0, end) + "`" + line.substring(end);
            }
          }
          return line;
        }
      },
      // Single underscore italic (_) completion
      {
        name: "singleUnderscoreItalic",
        pattern: /[\s\S]*/,
        skipInBlockTypes: ["code", "math"],
        handler: ({ line }) => {
          let singleUnderscores = 0;
          for (let i = 0; i < line.length; i++) {
            if (line[i] === "_") {
              const prevChar = i > 0 ? line[i - 1] : "";
              const nextChar = i < line.length - 1 ? line[i + 1] : "";
              if (prevChar === "\\") continue;
              if (prevChar && nextChar && /[\p{L}\p{N}_]/u.test(prevChar) && /[\p{L}\p{N}_]/u.test(nextChar)) {
                continue;
              }
              if (prevChar !== "_" && nextChar !== "_") {
                singleUnderscores++;
              }
            }
          }
          if (singleUnderscores % 2 === 1) {
            let firstIdx = -1;
            for (let i = 0; i < line.length; i++) {
              if (line[i] === "_" && line[i - 1] !== "_" && line[i + 1] !== "_" && line[i - 1] !== "\\") {
                const prevChar = i > 0 ? line[i - 1] : "";
                const nextChar = i < line.length - 1 ? line[i + 1] : "";
                if (prevChar && nextChar && /[\p{L}\p{N}_]/u.test(prevChar) && /[\p{L}\p{N}_]/u.test(nextChar)) {
                  continue;
                }
                firstIdx = i;
                break;
              }
            }
            if (firstIdx !== -1) {
              const end = findEndOfCellOrLineContaining(line, firstIdx);
              return line.substring(0, end) + "_" + line.substring(end);
            }
          }
          return line;
        }
      },
      // Links and images completion
      {
        name: "linksAndImages",
        pattern: /(!?\[.*)$/,
        skipInBlockTypes: ["code", "math"],
        handler: ({ line }) => {
          const urlMatch = line.match(/(!?\[[^\]]*\]\()([^)]*?)$/);
          if (urlMatch) {
            const url = urlMatch[2];
            if (url.length > 0) {
              let isIncomplete = true;
              if (url.length >= 4) {
                if (url.startsWith("http://") && url.length >= 12 || url.startsWith("https://") && url.length >= 13) {
                  let domain = url;
                  if (url.startsWith("http://")) domain = url.substring(7);
                  else if (url.startsWith("https://"))
                    domain = url.substring(8);
                  domain = domain.split("/")[0].split("?")[0].split("#")[0];
                  const parts = domain.split(".");
                  if (parts.length >= 2) {
                    const ext = parts[parts.length - 1];
                    if (ext.length >= 2 && /^[a-zA-Z]+$/.test(ext)) {
                      isIncomplete = false;
                    }
                  }
                }
              }
              if (isIncomplete) {
                const marker = urlMatch[1].startsWith("!") ? "streamdown:incomplete-image" : "streamdown:incomplete-link";
                return line.replace(url, marker) + ")";
              } else {
                return line + ")";
              }
            } else {
              const marker = urlMatch[1].startsWith("!") ? "streamdown:incomplete-image" : "streamdown:incomplete-link";
              return line + marker + ")";
            }
          }
          const linkMatch = line.match(/(!?\[)([^\]]*?)$/);
          if (linkMatch && !line.includes("](")) {
            const [, openBracket, linkText] = linkMatch;
            const bracketIndex = line.lastIndexOf(openBracket);
            const end = findEndOfCellOrLineContaining(line, bracketIndex);
            const cleanText = linkText.replace(/[\s|]+$/, "");
            const marker = openBracket.startsWith("!") ? "streamdown:incomplete-image" : "streamdown:incomplete-link";
            const includeBoundary = end < line.length && line[end] === "|";
            const incompleteEnd = includeBoundary ? end + 1 : end;
            const incompletePart = line.substring(bracketIndex, incompleteEnd);
            const completedPart = openBracket + cleanText + "](" + marker + ")" + (includeBoundary ? "|" : "");
            return line.replace(incompletePart, completedPart);
          }
          return line;
        }
      }
    ];
  }
};
function parseIncompleteMarkdown(text) {
  const parser = new IncompleteMarkdownParser(
    IncompleteMarkdownParser.createDefaultPlugins()
  );
  return parser.parse(text);
}

// src/parser.ts
var processor = unified().use(remarkParse).use(remarkGfm).use(remarkRehype, { allowDangerousHtml: false });
function parseMarkdown(content, options) {
  if (!content) return null;
  const prepared = options?.isStreaming ? parseIncompleteMarkdown(content) : content;
  const mdast = processor.runSync(processor.parse(prepared));
  return toJsxRuntime(mdast, {
    jsx,
    jsxs,
    Fragment,
    components: options?.components
  });
}

// src/utils/parse-blocks.ts
function parseMarkdownIntoBlocks(markdown) {
  if (!markdown) return [];
  const lines = markdown.split("\n");
  const blocks = [];
  let currentBlock = [];
  let inCodeFence = false;
  let codeFenceChar = "";
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    const trimmed = line.trim();
    if (!inCodeFence) {
      if (trimmed.startsWith("```") || trimmed.startsWith("~~~")) {
        inCodeFence = true;
        codeFenceChar = trimmed[0];
        currentBlock.push(line);
        continue;
      }
    } else {
      if (trimmed.startsWith(codeFenceChar.repeat(3)) && trimmed.replace(new RegExp(`^\\${codeFenceChar}+`), "").trim() === "") {
        inCodeFence = false;
        currentBlock.push(line);
        continue;
      }
      currentBlock.push(line);
      continue;
    }
    if (trimmed === "") {
      if (currentBlock.length > 0) {
        blocks.push(currentBlock.join("\n"));
        currentBlock = [];
      }
    } else {
      currentBlock.push(line);
    }
  }
  if (currentBlock.length > 0 || inCodeFence) {
    blocks.push(currentBlock.join("\n"));
  }
  return blocks;
}

// src/stream-markdown.tsx
var _tmpl$ = /* @__PURE__ */ template(`<div>`);
var _tmpl$2 = /* @__PURE__ */ template(`<div class=streamdown-block>`);
var StreamMarkdown = (props) => {
  const markdown = createMemo(() => {
    if (props.stream) {
      return props.stream.content();
    }
    return props.content ?? "";
  });
  const isStreaming = createMemo(() => {
    return props.stream?.isStreaming() ?? false;
  });
  const blocks = createMemo(() => {
    return parseMarkdownIntoBlocks(markdown());
  });
  const blockCache = /* @__PURE__ */ new Map();
  const renderBlock = (block, index) => {
    const isLastBlock = index === blocks().length - 1;
    const isActive = isStreaming() && isLastBlock;
    if (!isActive && blockCache.has(block)) {
      return blockCache.get(block);
    }
    const rendered = parseMarkdown(block, {
      components: props.components,
      isStreaming: isActive
    });
    if (!isActive) {
      blockCache.set(block, rendered);
    }
    return rendered;
  };
  return (() => {
    var _el$ = _tmpl$();
    insert(_el$, createComponent(For, {
      get each() {
        return blocks();
      },
      children: (block, index) => (() => {
        var _el$2 = _tmpl$2();
        insert(_el$2, () => renderBlock(block, index()));
        return _el$2;
      })()
    }));
    effect((_p$) => {
      var _v$ = `${props.class || ""} streamdown`, _v$2 = !!isStreaming(), _v$3 = !!(isStreaming() && (props.showCaret ?? true));
      _v$ !== _p$.e && className(_el$, _p$.e = _v$);
      _v$2 !== _p$.t && _el$.classList.toggle("streamdown-streaming", _p$.t = _v$2);
      _v$3 !== _p$.a && _el$.classList.toggle("streamdown-caret", _p$.a = _v$3);
      return _p$;
    }, {
      e: void 0,
      t: void 0,
      a: void 0
    });
    return _el$;
  })();
};
function createMarkdownStream(options) {
  const [content, setContent] = createSignal(options?.initialContent ?? "");
  const [isStreaming, setIsStreaming] = createSignal(true);
  return {
    write(chunk) {
      setContent((prev) => prev + chunk);
    },
    end() {
      setIsStreaming(false);
    },
    content,
    isStreaming,
    reset() {
      setContent(options?.initialContent ?? "");
      setIsStreaming(true);
    }
  };
}

export { IncompleteMarkdownParser, StreamMarkdown, createMarkdownStream, parseIncompleteMarkdown, parseMarkdownIntoBlocks };
